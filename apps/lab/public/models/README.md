# LAB-served model copies

Files placed here are served by Vite under `/models/`.

Example:

```text
apps/lab/public/models/podocyte.glb
→
/models/podocyte.glb
```

Use this folder only for stable development-serving copies. Canonical exported assets remain in `assets/glb/`.
