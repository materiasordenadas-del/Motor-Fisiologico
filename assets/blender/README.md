# Blender source assets

Store editable `.blend` source files here.

Recommended structure:

```text
assets/blender/<asset-id>/source.blend
```

Blender files are authoring sources, not physiological authorities. Exported web-ready models belong in `assets/glb/`.
