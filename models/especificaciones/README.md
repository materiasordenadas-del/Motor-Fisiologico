# Especificaciones locales

Una especificación registra únicamente lo que cambia respecto de uno o más principios generales. No debe copiar el modelo basal completo.

Los archivos deben poder mapearse a `LocalSpecification` de `packages/contracts` y conservar fuentes.
