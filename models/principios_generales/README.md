# Principios generales

Una regla solo entra aquí si es universal dentro de su `scope` declarado y bajo sus precondiciones declaradas. Si existe una excepción real dentro del scope, se reduce el scope o la regla pasa a especificaciones.

Los archivos deben poder mapearse a `GeneralPrinciple` de `packages/contracts` y conservar fuentes.
